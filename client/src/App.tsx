import LoginPage from './pages/LoginPage'

/**
 * App 컴포넌트
 * - 애플리케이션의 루트 컴포넌트
 * - 현재는 디자인 프리뷰를 위해 로그인 페이지를 직접 렌더링
 * - 추후 라우터 도입 시 여기서 라우팅 설정
 */
function App() {
  return <LoginPage />
}

export default App
